export const styles = {
    control: (styles) => ({
        ...styles,
        width: "250px",
        height: "50px",
        fontSize: "16px",
    }),
    option: (styles) => ({
        ...styles,
        fontSize: "16px",
    }),
}